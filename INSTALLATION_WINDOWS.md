# 🪟 Пошаговая инструкция по установке для Windows

## Что вам понадобится

Для запуска проекта на Windows нужно установить 3 программы:
1. Java JDK (для разработки на Java)
2. Apache Maven (для управления проектом)
3. MySQL Server (база данных)

**Время установки:** примерно 30-40 минут

---

## 📥 Шаг 1: Установка Java JDK 11

### 1.1 Скачивание

1. Откройте браузер и перейдите на сайт: **https://www.oracle.com/java/technologies/downloads/**
2. Найдите раздел **Java 11** (или Java 17 LTS)
3. Нажмите на вкладку **Windows**
4. Скачайте файл **x64 Installer** (например: `jdk-11.0.20_windows-x64_bin.exe`)
   - Размер файла примерно 150-160 MB

### 1.2 Установка

1. Запустите скачанный файл `.exe`
2. Нажмите **Next** (Далее)
3. Оставьте путь установки по умолчанию (обычно `C:\Program Files\Java\jdk-11`)
   - **ВАЖНО:** Запомните этот путь!
4. Нажмите **Next** и дождитесь завершения установки
5. Нажмите **Close** (Закрыть)

### 1.3 Настройка переменных окружения

**Это важный шаг! Без него Java не будет работать!**

1. Нажмите правой кнопкой мыши на **"Этот компьютер"** (или "Мой компьютер")
2. Выберите **"Свойства"**
3. Нажмите **"Дополнительные параметры системы"** (слева)
4. Нажмите кнопку **"Переменные среды..."** внизу

#### Создание переменной JAVA_HOME:

5. В разделе **"Системные переменные"** нажмите **"Создать..."**
6. Введите:
   - **Имя переменной:** `JAVA_HOME`
   - **Значение переменной:** `C:\Program Files\Java\jdk-11` (или ваш путь установки)
7. Нажмите **ОК**

#### Добавление Java в PATH:

8. В разделе **"Системные переменные"** найдите переменную **Path**
9. Выберите её и нажмите **"Изменить..."**
10. Нажмите **"Создать"**
11. Добавьте: `%JAVA_HOME%\bin`
12. Нажмите **ОК** во всех открытых окнах

### 1.4 Проверка установки

1. Откройте **Командную строку** (Win + R → введите `cmd` → Enter)
2. Введите команды:
```cmd
java -version
```
Должно появиться что-то вроде:
```
java version "11.0.20" 2023-07-18 LTS
```

3. Проверьте компилятор:
```cmd
javac -version
```
Должно появиться:
```
javac 11.0.20
```

✅ Если видите версии - Java установлена правильно!
❌ Если ошибка "не является внутренней командой" - проверьте переменные окружения

---

## 📥 Шаг 2: Установка Apache Maven

### 2.1 Скачивание

1. Откройте браузер: **https://maven.apache.org/download.cgi**
2. Найдите раздел **"Files"**
3. Скачайте **Binary zip archive** (например: `apache-maven-3.9.5-bin.zip`)
   - Размер примерно 9 MB

### 2.2 Установка

1. Откройте скачанный ZIP архив
2. Извлеките папку в удобное место, например:
   - `C:\Program Files\Apache\apache-maven-3.9.5`
   - **ВАЖНО:** Путь не должен содержать пробелов и русских букв!

### 2.3 Настройка переменных окружения

1. Откройте **"Переменные среды"** (как в шаге 1.3)

#### Создание переменной MAVEN_HOME:

2. В **"Системные переменные"** нажмите **"Создать..."**
3. Введите:
   - **Имя переменной:** `MAVEN_HOME`
   - **Значение переменной:** `C:\Program Files\Apache\apache-maven-3.9.5`
4. Нажмите **ОК**

#### Добавление Maven в PATH:

5. Найдите переменную **Path** и нажмите **"Изменить..."**
6. Нажмите **"Создать"**
7. Добавьте: `%MAVEN_HOME%\bin`
8. Нажмите **ОК** во всех окнах

### 2.4 Проверка установки

1. **ВАЖНО:** Закройте старую командную строку и откройте новую!
2. Введите:
```cmd
mvn -version
```
Должно появиться:
```
Apache Maven 3.9.5
Maven home: C:\Program Files\Apache\apache-maven-3.9.5
Java version: 11.0.20, vendor: Oracle Corporation
```

✅ Maven установлен правильно!

---

## 📥 Шаг 3: Установка MySQL Server 8.0

### 3.1 Скачивание

1. Откройте: **https://dev.mysql.com/downloads/mysql/**
2. Выберите **Windows** в выпадающем списке "Select Operating System"
3. Скачайте **MySQL Installer for Windows**
   - Рекомендуется: `mysql-installer-community-8.0.xx.x.msi` (полная версия ~400 MB)
4. Нажмите **Download** (может потребоваться создать аккаунт Oracle, но можно пропустить через "No thanks, just start my download")

### 3.2 Установка

1. Запустите скачанный `.msi` файл
2. Выберите тип установки: **Developer Default** (включает сервер и полезные инструменты)
3. Нажмите **Next**
4. Если появится окно с требованиями - нажмите **Execute** для установки зависимостей
5. Нажмите **Next** → **Execute** для начала установки
6. Дождитесь завершения (может занять 5-10 минут)
7. Нажмите **Next**

### 3.3 Конфигурация MySQL Server

#### Product Configuration:

1. Нажмите **Next**

#### Type and Networking:

2. Оставьте все по умолчанию:
   - **Config Type:** Development Computer
   - **Port:** 3306
3. Нажмите **Next**

#### Authentication Method:

4. Выберите: **Use Strong Password Encryption** (рекомендуется)
5. Нажмите **Next**

#### Accounts and Roles (САМЫЙ ВАЖНЫЙ ШАГ!):

6. Установите пароль для пользователя **root**:
   - Введите пароль (например: `root` или `password123`)
   - **❗ ЗАПОМНИТЕ ЭТОТ ПАРОЛЬ! Он понадобится для проекта!**
7. Повторите пароль
8. Нажмите **Next**

#### Windows Service:

9. Отметьте:
   - ✅ **Configure MySQL Server as a Windows Service**
   - ✅ **Start the MySQL Server at System Startup**
10. Нажмите **Next**

#### Server File Permissions:

11. Оставьте по умолчанию
12. Нажмите **Next**

#### Apply Configuration:

13. Нажмите **Execute**
14. Дождитесь выполнения всех шагов (зелёные галочки)
15. Нажмите **Finish**

#### Остальные продукты:

16. Продолжайте нажимать **Next** и **Finish** для остальных продуктов (Router, Samples)
17. В конце нажмите **Finish**

### 3.4 Проверка установки

1. Откройте **Командную строку**
2. Введите:
```cmd
mysql --version
```
Должно появиться:
```
mysql  Ver 8.0.xx for Win64 on x86_64
```

3. Проверьте подключение:
```cmd
mysql -u root -p
```
4. Введите ваш пароль (тот, что установили в шаге 3.3)
5. Если появилась строка `mysql>` - всё работает!
6. Введите `exit` для выхода

✅ MySQL установлен и работает!

---

## 📥 Шаг 4 (Опционально): Установка MySQL Workbench

MySQL Workbench - это графический интерфейс для работы с базой данных. Очень удобно!

### 4.1 Установка

Если вы устанавливали MySQL через MySQL Installer (шаг 3), то Workbench уже установлен!

### 4.2 Запуск

1. Найдите в меню **Пуск**: **MySQL Workbench**
2. Запустите программу
3. В разделе **MySQL Connections** увидите подключение **Local instance MySQL80**
4. Нажмите на него
5. Введите пароль root
6. Готово! Вы в графическом интерфейсе MySQL

---

## 🚀 Шаг 5: Настройка проекта

### 5.1 Скачивание проекта

Если проект на GitHub:
```cmd
cd C:\Users\ВашеИмя\Documents
git clone <URL_репозитория>
cd javalab
```

Или просто скачайте ZIP архив проекта и распакуйте.

### 5.2 Создание базы данных

#### Способ 1 - через командную строку:

1. Откройте **Командную строку**
2. Перейдите в папку проекта:
```cmd
cd C:\Users\ВашеИмя\Documents\javalab
```
3. Выполните команду:
```cmd
mysql -u root -p < src\main\resources\init.sql
```
4. Введите пароль root

#### Способ 2 - через MySQL Workbench (ПРОЩЕ!):

1. Откройте **MySQL Workbench**
2. Подключитесь к серверу (введите пароль root)
3. Нажмите **File** → **Open SQL Script**
4. Найдите файл: `C:\Users\ВашеИмя\Documents\javalab\src\main\resources\init.sql`
5. Нажмите кнопку **⚡ Execute** (молния вверху)
6. Готово! База данных создана!

### 5.3 Настройка подключения к БД

1. Откройте файл: `src\main\resources\database.properties`
2. Блокнотом или любым редактором кода
3. Измените строку с паролем:

```properties
db.driver=com.mysql.cj.jdbc.Driver
db.url=jdbc:mysql://localhost:3306/travel_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
db.user=root
db.password=ВАШ_ПАРОЛЬ_MYSQL    ← Замените на ваш пароль!
db.name=travel_db
```

4. Сохраните файл

### 5.4 Загрузка зависимостей и компиляция

1. Откройте **Командную строку**
2. Перейдите в папку проекта:
```cmd
cd C:\Users\ВашеИмя\Documents\javalab
```
3. Выполните команду:
```cmd
mvn clean install
```
4. Дождитесь завершения (первый раз может занять несколько минут - Maven скачивает библиотеки)
5. В конце должно быть: **BUILD SUCCESS**

✅ Проект готов к запуску!

---

## ▶️ Шаг 6: Запуск проекта

### Способ 1 - через командную строку:

```cmd
cd C:\Users\ВашеИмя\Documents\javalab
mvn exec:java -Dexec.mainClass="by.bsuir.travel.Main"
```

### Способ 2 - через IntelliJ IDEA:

1. Скачайте **IntelliJ IDEA Community** (бесплатная): https://www.jetbrains.com/idea/download/
2. Установите программу
3. Запустите IntelliJ IDEA
4. Нажмите **Open**
5. Выберите папку проекта `javalab`
6. Дождитесь, пока IDEA загрузит проект (внизу справа - прогресс-бар)
7. Откройте файл: `src/main/java/by/bsuir/travel/Main.java`
8. Нажмите правой кнопкой мыши на файл → **Run 'Main.main()'**

### Способ 3 - через Eclipse:

1. Скачайте **Eclipse IDE for Java Developers**: https://www.eclipse.org/downloads/
2. Установите
3. Откройте Eclipse
4. **File** → **Import** → **Existing Maven Projects**
5. Выберите папку `javalab`
6. Нажмите **Finish**
7. Откройте `Main.java`
8. Правой кнопкой → **Run As** → **Java Application**

---

## ✅ Проверка работы

Если всё настроено правильно, в консоли появится:

```
================================================================================
КОНТРОЛЬНАЯ РАБОТА - ВАРИАНТ 5
Туристическое агентство - Управление клиентами
================================================================================

1. ТЕСТ МЕТОДА findAll() - Получение всех туристов:
--------------------------------------------------------------------------------
Travel{id=1, firstName='Иван', lastName='Петров', email='ivan.petrov@mail.ru'...
Travel{id=2, firstName='Мария', lastName='Сидорова', email='maria.sidorova@gmail.com'...
...
```

🎉 **Поздравляем! Проект работает!**

---

## ❌ Возможные проблемы и решения

### Проблема 1: "java не является внутренней командой"

**Решение:**
- Проверьте переменную окружения JAVA_HOME
- Убедитесь, что `%JAVA_HOME%\bin` добавлен в Path
- **Обязательно закройте и откройте командную строку заново!**

### Проблема 2: "mvn не является внутренней командой"

**Решение:**
- Проверьте переменную MAVEN_HOME
- Убедитесь, что `%MAVEN_HOME%\bin` добавлен в Path
- Закройте и откройте командную строку

### Проблема 3: "Communications link failure"

**Решение:**
- Проверьте, что служба MySQL запущена:
  1. Win + R → введите `services.msc` → Enter
  2. Найдите **MySQL80** в списке
  3. Если статус не "Работает" - нажмите правой кнопкой → **Запустить**

### Проблема 4: "Access denied for user 'root'@'localhost'"

**Решение:**
- Откройте файл `src\main\resources\database.properties`
- Измените `db.password=` на ваш правильный пароль MySQL

### Проблема 5: "Unknown database 'travel_db'"

**Решение:**
- База данных не создана
- Выполните SQL скрипт (см. Шаг 5.2)

### Проблема 6: Maven долго скачивает файлы

**Это нормально!**
- При первом запуске Maven скачивает все необходимые библиотеки
- Может занять 5-15 минут в зависимости от интернета
- Последующие запуски будут быстрыми

---

## 📋 Контрольный список

Перед запуском проекта проверьте:

- [ ] Установлена Java JDK 11+
- [ ] Команда `java -version` работает
- [ ] Установлен Maven
- [ ] Команда `mvn -version` работает
- [ ] Установлен MySQL Server
- [ ] Служба MySQL80 запущена (в services.msc)
- [ ] Вы знаете пароль root для MySQL
- [ ] Выполнен SQL скрипт `init.sql`
- [ ] В файле `database.properties` правильный пароль
- [ ] Выполнена команда `mvn clean install`
- [ ] BUILD SUCCESS!

---

## 💡 Полезные советы

### Как открыть командную строку в папке проекта:

1. Откройте папку проекта в Проводнике Windows
2. В адресной строке (где путь к папке) введите: `cmd`
3. Нажмите Enter
4. Командная строка откроется сразу в нужной папке!

### Как проверить, работает ли MySQL:

```cmd
sc query MySQL80
```
Должно быть: `STATE: 4 RUNNING`

### Как перезапустить MySQL:

```cmd
net stop MySQL80
net start MySQL80
```

### Горячие клавиши в IntelliJ IDEA:

- **Ctrl + F9** - Скомпилировать проект
- **Shift + F10** - Запустить программу
- **Ctrl + Shift + F10** - Запустить текущий файл

---

## 📞 Что делать, если ничего не работает?

1. Перечитайте инструкцию внимательно
2. Проверьте контрольный список
3. Убедитесь, что все переменные окружения установлены
4. Перезагрузите компьютер (серьёзно, иногда это помогает!)
5. Проверьте логи ошибок в командной строке

---

## 🎓 Успехов в выполнении контрольной работы!

Если вы выполнили все шаги - у вас всё должно работать!

**Не торопитесь, следуйте инструкциям пошагово!** 🚀
