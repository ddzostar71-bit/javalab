# CI/CD Pipeline - GitHub Actions

## Обзор

Проект использует **GitHub Actions** для реализации полного цикла непрерывной интеграции и развертывания (CI/CD). Автоматизация позволяет:

- Автоматически тестировать код при каждом коммите
- Проверять работоспособность проекта в изолированной среде
- Генерировать отчеты о выполнении
- Обеспечивать воспроизводимость результатов

---

## Архитектура пайплайна

### Workflow файл: `.github/workflows/test-project.yml`

Пайплайн описывается в YAML-формате и содержит определение задач (jobs) и шагов (steps) для их выполнения.

### Триггеры запуска

Workflow автоматически запускается при следующих событиях:

```yaml
on:
  push:
    branches: [ "**" ]           # При push в любую ветку
  pull_request:
    branches: [ "**" ]           # При создании Pull Request
  workflow_dispatch:             # Ручной запуск через интерфейс GitHub
```

**Преимущества:**
- Каждое изменение кода автоматически проверяется
- Возможность запуска вручную для демонстрации
- Проверка кода перед слиянием веток

---

## Окружение выполнения

```yaml
runs-on: ubuntu-latest
```

Пайплайн выполняется на виртуальной машине с последней версией Ubuntu Linux, что обеспечивает:
- Чистую среду для каждого запуска
- Отсутствие зависимости от локального окружения
- Быструю установку всех необходимых компонентов

---

## Этапы пайплайна (Steps)

### 1. Клонирование репозитория

```yaml
- name: 📥 Клонирование репозитория
  uses: actions/checkout@v4
```

**Что происходит:**
- GitHub Actions клонирует исходный код проекта
- Извлекается конкретный коммит, который инициировал запуск
- Весь код становится доступным для последующих шагов

**Action:** `actions/checkout@v4` - официальный action от GitHub для клонирования репозиториев

---

### 2. Установка Java JDK

```yaml
- name: ☕ Установка Java JDK 11
  uses: actions/setup-java@v4
  with:
    java-version: '11'
    distribution: 'temurin'
    cache: maven
```

**Что происходит:**
- Устанавливается Java Development Kit версии 11
- Используется дистрибутив **Temurin** (Eclipse Adoptium) - открытая и стабильная реализация OpenJDK
- Включается кэширование Maven зависимостей для ускорения последующих запусков

**Параметры:**
- `java-version: '11'` - соответствует требованиям проекта
- `distribution: 'temurin'` - бесплатная, качественная реализация JDK
- `cache: maven` - кэширует `~/.m2/repository` для ускорения загрузки зависимостей

**Время выполнения:** ~10-15 секунд (с кэшем), ~30-60 секунд (первый раз)

---

### 3. Установка и запуск MySQL

```yaml
- name: 🗄️ Установка MySQL
  run: |
    sudo systemctl start mysql.service
    mysql -e "SELECT VERSION();" -uroot -proot
```

**Что происходит:**
- Запускается предустановленный на Ubuntu MySQL Server
- Проверяется успешность подключения и версия БД
- Используется root пользователь с паролем root (стандартно в GitHub Actions)

**Технические детали:**
- MySQL уже предустановлен в образе `ubuntu-latest`
- Служба запускается через systemd
- Выполняется тестовый SQL-запрос для проверки работоспособности

**Вывод:**
```
VERSION()
8.0.xx
```

---

### 4. Создание базы данных

```yaml
- name: 📊 Создание базы данных
  run: |
    mysql -uroot -proot < src/main/resources/init.sql
    echo "✅ База данных создана успешно!"
    mysql -uroot -proot -e "USE travel_db; SELECT COUNT(*) as 'Количество туристов' FROM tourists;"
```

**Что происходит:**
1. Выполняется SQL-скрипт инициализации из `src/main/resources/init.sql`
2. Создается база данных `travel_db`
3. Создается таблица `tourists` с необходимой структурой
4. Заполняются тестовые данные (если есть в скрипте)
5. Проверяется количество записей в таблице

**SQL-скрипт выполняет:**
```sql
CREATE DATABASE IF NOT EXISTS travel_db;
USE travel_db;
CREATE TABLE tourists (...);
INSERT INTO tourists VALUES (...);
```

**Вывод:**
```
✅ База данных создана успешно!
Количество туристов
5
```

---

### 5. Настройка подключения к БД

```yaml
- name: ⚙️ Настройка подключения к БД
  run: |
    sed -i 's/db.password=root/db.password=root/g' src/main/resources/database.properties
    echo "✅ Конфигурация обновлена"
```

**Что происходит:**
- Используется утилита `sed` для замены параметров в конфигурационном файле
- Обновляется пароль для подключения к MySQL (если требуется)
- Гарантируется соответствие настроек окружению GitHub Actions

**Альтернативный подход:**
В реальных проектах обычно используются секреты GitHub:
```yaml
env:
  DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
```

---

### 6. Компиляция проекта

```yaml
- name: 🔨 Компиляция проекта с Maven
  run: |
    mvn clean compile
    echo "✅ Проект скомпилирован успешно!"
```

**Что происходит:**
1. `mvn clean` - очищает директорию `target` от предыдущих сборок
2. `mvn compile` - компилирует исходный код проекта
3. Maven автоматически загружает все зависимости из `pom.xml` (MySQL Connector/J)
4. Проверяется отсутствие ошибок компиляции

**Загружаемые зависимости:**
```xml
<dependency>
    <groupId>mysql</groupId>
    <artifactId>mysql-connector-java</artifactId>
    <version>8.0.33</version>
</dependency>
```

**Результат:**
- Скомпилированные `.class` файлы в `target/classes/`
- Все зависимости в локальном репозитории Maven

**Время выполнения:** ~20-40 секунд

---

### 7. Запуск приложения с фильтрацией вывода

```yaml
- name: 🚀 Запуск главного класса и фильтрация вывода
  run: |
    mvn exec:java -Dexec.mainClass="by.bsuir.travel.Main" 2>&1 | \
    grep -v "^\[INFO\]" | \
    grep -v "^\[WARNING\]" | \
    grep -v "^Download" | \
    grep -v "^Progress" | \
    sed '/^$/N;/^\n$/D' > clean_output.txt
    echo "✅ Программа выполнена, вывод сохранен"
```

**Что происходит:**

#### Запуск приложения
```bash
mvn exec:java -Dexec.mainClass="by.bsuir.travel.Main"
```
- Выполняется главный класс `Main.java`
- Maven plugin `exec-maven-plugin` запускает приложение в JVM

#### Фильтрация вывода
Используется цепочка команд для очистки логов:

1. **`2>&1`** - объединяет stderr и stdout в один поток
2. **`grep -v "^\[INFO\]"`** - убирает строки начинающиеся с [INFO]
3. **`grep -v "^\[WARNING\]"`** - убирает строки с [WARNING]
4. **`grep -v "^Download"`** - убирает сообщения о загрузке
5. **`grep -v "^Progress"`** - убирает прогресс-бары
6. **`sed '/^$/N;/^\n$/D'`** - убирает лишние пустые строки
7. **`> clean_output.txt`** - сохраняет результат в файл

**Результат:**
Файл `clean_output.txt` содержит только чистый вывод программы без технических логов Maven:

```
================================================================================
КОНТРОЛЬНАЯ РАБОТА - ВАРИАНТ 5
Туристическое агентство - Управление клиентами
================================================================================

1. ТЕСТ МЕТОДА findAll() - Получение всех туристов:
...
```

---

### 8. Создание отчета о выполнении

```yaml
- name: 📋 Создание отчета о выполнении
  run: |
    cat > CONTROL_WORK_REPORT.txt << 'EOF'
    [... содержимое отчета ...]
    EOF
```

**Что происходит:**

Создается структурированный отчет в текстовом формате, который включает:

1. **Заголовок и метаданные:**
   - Название дисциплины и темы работы
   - Дата и время выполнения (генерируется командой `date`)

2. **Описание задания:**
   - Полное описание варианта 5
   - Список всех 6 требуемых методов с описанием

3. **Демонстрация работы:**
   - Содержимое файла `clean_output.txt`
   - Чистый вывод программы без технических логов

4. **Результаты и заключение:**
   - Список выполненных требований (с галочками ✅)
   - Заключение о готовности работы

**Техника создания:**
```bash
cat > file.txt << 'EOF'
содержимое
EOF
```
- `cat >` - перенаправляет вывод в файл
- `<< 'EOF'` - heredoc синтаксис для многострочного текста
- Одинарные кавычки в `'EOF'` предотвращают интерполяцию переменных

**Преимущества:**
- Преподаватель получает готовый отчет для проверки
- Все необходимые данные в одном файле
- Не требуется разбираться в технических логах

---

### 9. Загрузка отчета как артефакт

```yaml
- name: 📤 Загрузка отчета как артефакт
  uses: actions/upload-artifact@v4
  with:
    name: Control-Work-Report-Variant-5-${{ github.run_number }}
    path: |
      CONTROL_WORK_REPORT.txt
      clean_output.txt
    retention-days: 90
```

**Что происходит:**

GitHub Actions сохраняет файлы как **артефакты** (artifacts) - файлы, доступные для скачивания после завершения workflow.

**Параметры:**

- **`name`**: имя артефакта с уникальным номером запуска
  - `${{ github.run_number }}` - автоматическая переменная с номером запуска
  - Пример: `Control-Work-Report-Variant-5-42`

- **`path`**: список файлов для сохранения
  - `CONTROL_WORK_REPORT.txt` - структурированный отчет
  - `clean_output.txt` - чистый вывод программы

- **`retention-days: 90`**: срок хранения артефактов
  - Файлы доступны для скачивания 90 дней
  - После этого автоматически удаляются

**Как скачать артефакт:**
1. Перейти на вкладку "Actions" в репозитории
2. Открыть нужный запуск workflow
3. Прокрутить вниз до раздела "Artifacts"
4. Нажать на имя артефакта для скачивания ZIP-архива

**Action:** `actions/upload-artifact@v4` - официальный action для работы с артефактами

---

### 10. Вывод статистики и итогов

```yaml
- name: 📊 Вывод статистики и итогов
  run: |
    echo "╔════════════════════════════════════════════════════════╗"
    echo "║           КОНТРОЛЬНАЯ РАБОТА - ВАРИАНТ 5              ║"
    echo "║     Туристическое агентство - УСПЕШНО ВЫПОЛНЕНО!      ║"
    echo "╚════════════════════════════════════════════════════════╝"

    mysql -uroot -proot -e "USE travel_db;
    SELECT 'Всего туристов:' as 'Параметр', COUNT(*) as 'Значение' FROM tourists
    UNION ALL
    SELECT 'Средняя цена тура:', ROUND(AVG(price), 2) FROM tourists
    UNION ALL
    SELECT 'Средняя длительность:', ROUND(AVG(duration_days), 1) FROM tourists;"
```

**Что происходит:**

#### 1. Красивый ASCII-баннер
Выводится рамка с подтверждением успешного выполнения

#### 2. SQL-статистика базы данных
Выполняются агрегирующие запросы для вывода статистики:

```sql
SELECT 'Всего туристов:', COUNT(*) FROM tourists
```
- Подсчитывает общее количество записей

```sql
SELECT 'Средняя цена тура:', ROUND(AVG(price), 2)
```
- Вычисляет среднюю стоимость тура с округлением до 2 знаков

```sql
SELECT 'Средняя длительность:', ROUND(AVG(duration_days), 1)
```
- Вычисляет среднюю продолжительность тура

**Использование UNION ALL:**
Объединяет результаты трех запросов в одну таблицу для красивого вывода

**Пример вывода:**
```
╔════════════════════════════════════════════════════════╗
║           КОНТРОЛЬНАЯ РАБОТА - ВАРИАНТ 5              ║
║     Туристическое агентство - УСПЕШНО ВЫПОЛНЕНО!      ║
╚════════════════════════════════════════════════════════╝

Параметр                  | Значение
--------------------------|----------
Всего туристов:          | 5
Средняя цена тура:       | 1650.00
Средняя длительность:    | 8.2

✅ Все тесты пройдены успешно!
```

---

## Визуализация пайплайна

### Диаграмма последовательности выполнения

```
┌─────────────────────────────────────────────────────────┐
│ ТРИГГЕР: Push / Pull Request / Manual                  │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│ STEP 1: Клонирование репозитория                       │
│ ├─ Checkout исходного кода                            │
│ └─ Время: ~5 сек                                       │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│ STEP 2: Установка Java JDK 11                         │
│ ├─ Установка Temurin OpenJDK                          │
│ ├─ Настройка переменных окружения                     │
│ └─ Время: ~10-15 сек (с кэшем)                        │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│ STEP 3: Установка и запуск MySQL                      │
│ ├─ Запуск MySQL Server через systemd                  │
│ ├─ Проверка подключения                               │
│ └─ Время: ~5-10 сек                                    │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│ STEP 4: Создание базы данных                          │
│ ├─ Выполнение init.sql скрипта                        │
│ ├─ Создание таблиц                                    │
│ ├─ Вставка тестовых данных                            │
│ └─ Время: ~3-5 сек                                     │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│ STEP 5: Настройка конфигурации                        │
│ ├─ Обновление database.properties                     │
│ └─ Время: ~1 сек                                       │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│ STEP 6: Компиляция проекта                            │
│ ├─ Maven clean                                         │
│ ├─ Загрузка зависимостей                              │
│ ├─ Компиляция Java классов                            │
│ └─ Время: ~20-40 сек                                   │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│ STEP 7: Запуск приложения                             │
│ ├─ Выполнение Main.java                               │
│ ├─ Фильтрация вывода от Maven логов                   │
│ ├─ Сохранение в clean_output.txt                      │
│ └─ Время: ~5-10 сек                                    │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│ STEP 8: Создание отчета                               │
│ ├─ Формирование структурированного отчета              │
│ ├─ Добавление метаданных                              │
│ ├─ Включение результатов выполнения                   │
│ └─ Время: ~1-2 сек                                     │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│ STEP 9: Загрузка артефактов                           │
│ ├─ Упаковка отчетов в ZIP                             │
│ ├─ Сохранение на GitHub (90 дней)                     │
│ └─ Время: ~2-3 сек                                     │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│ STEP 10: Вывод статистики                             │
│ ├─ SQL-запросы для статистики БД                      │
│ ├─ Финальное подтверждение                            │
│ └─ Время: ~2-3 сек                                     │
└────────────────────┬────────────────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────────────────┐
│ РЕЗУЛЬТАТ: ✅ УСПЕХ / ❌ ОШИБКА                        │
│ Общее время выполнения: ~1-2 минуты                   │
└─────────────────────────────────────────────────────────┘
```

---

## Преимущества автоматизации

### 1. Воспроизводимость
- Каждый запуск выполняется в идентичных условиях
- Исключается влияние локального окружения
- Гарантируется одинаковый результат

### 2. Непрерывная проверка качества
- Код тестируется при каждом изменении
- Ошибки обнаруживаются сразу после коммита
- Поддерживается стабильность проекта

### 3. Документирование
- Автоматическая генерация отчетов
- История всех запусков доступна в GitHub
- Легко отследить изменения и их влияние

### 4. Профессиональный подход
- Соответствует современным практикам разработки
- Демонстрирует владение CI/CD инструментами
- Упрощает код-ревью и проверку работы

### 5. Удобство для преподавателя
- Не требуется локальная установка всех компонентов
- Один клик для проверки работоспособности
- Готовые отчеты для оценки

---

## Возможности расширения

### Добавление юнит-тестов
```yaml
- name: 🧪 Запуск тестов
  run: mvn test
```

### Статический анализ кода
```yaml
- name: 🔍 Статический анализ
  run: mvn checkstyle:check
```

### Проверка покрытия кода
```yaml
- name: 📊 Покрытие тестами
  run: mvn jacoco:report
```

### Деплой артефактов
```yaml
- name: 📦 Создание JAR
  run: mvn package
```

### Интеграция с SonarQube
```yaml
- name: 🔬 Анализ качества кода
  run: mvn sonar:sonar
```

---

## Мониторинг и отладка

### Просмотр логов в реальном времени
1. Перейдите на вкладку **Actions**
2. Откройте активный workflow (желтый кружок)
3. Нажмите на название job
4. Логи отображаются в реальном времени

### Отладка ошибок
Если workflow завершается с ошибкой:
1. Откройте логи failed step
2. Найдите красный текст с описанием ошибки
3. Проверьте предыдущие шаги на наличие предупреждений

### Re-run workflow
Можно перезапустить failed workflow:
- Нажмите "Re-run jobs" → "Re-run failed jobs"
- Или "Re-run all jobs" для полного перезапуска

---

## Best Practices реализованные в пайплайне

### ✅ Использование официальных Actions
- `actions/checkout@v4`
- `actions/setup-java@v4`
- `actions/upload-artifact@v4`

### ✅ Кэширование зависимостей
```yaml
cache: maven
```
Ускоряет последующие запуски на 30-50%

### ✅ Логическое разделение шагов
Каждый step отвечает за одну задачу, что упрощает отладку

### ✅ Информативные имена
Каждый step имеет понятное описание с эмодзи для визуальной идентификации

### ✅ Проверка результатов
После каждой критичной операции выполняется проверка успешности

### ✅ Артефакты для длительного хранения
Отчеты доступны 90 дней, даже после удаления веток

### ✅ Чистый вывод
Фильтрация технических логов делает результаты понятными

---

## Заключение

GitHub Actions пайплайн обеспечивает:
- **Автоматизацию** тестирования проекта
- **Валидацию** корректности реализации
- **Документирование** результатов работы
- **Профессиональный** подход к разработке

Это современный стандарт в индустрии разработки ПО, демонстрирующий владение практиками DevOps и CI/CD.
