name: Интерактивное тестирование контрольной работы

on:
  workflow_dispatch:  # Только ручной запуск с параметрами
    inputs:
      variant_number:
        description: 'Номер варианта контрольной работы'
        required: true
        default: '5'
        type: string

      search_id:
        description: 'ID туриста для поиска (метод findEntityById)'
        required: true
        default: '1'
        type: string

      # Параметры нового туриста для создания
      tourist_first_name:
        description: 'Имя нового туриста'
        required: true
        default: 'Анна'
        type: string

      tourist_last_name:
        description: 'Фамилия нового туриста'
        required: true
        default: 'Васильева'
        type: string

      tourist_email:
        description: 'Email нового туриста'
        required: true
        default: 'anna.vasilyeva@mail.ru'
        type: string

      tourist_phone:
        description: 'Телефон нового туриста'
        required: true
        default: '+375291234572'
        type: string

      tourist_destination:
        description: 'Направление тура'
        required: true
        default: 'Венеция'
        type: string

      tourist_duration:
        description: 'Длительность тура (дней)'
        required: true
        default: '8'
        type: string

      tourist_price:
        description: 'Цена тура'
        required: true
        default: '1900.00'
        type: string

      # Параметры для обновления туриста
      update_destination:
        description: 'Новое направление для обновления'
        required: true
        default: 'Милан'
        type: string

      update_duration:
        description: 'Новая длительность для обновления (дней)'
        required: true
        default: '10'
        type: string

      update_price:
        description: 'Новая цена для обновления'
        required: true
        default: '2100.00'
        type: string

      # Параметры базы данных
      db_name:
        description: 'Имя базы данных'
        required: true
        default: 'travel_db'
        type: string

      db_user:
        description: 'Пользователь БД'
        required: true
        default: 'root'
        type: string

      db_password:
        description: 'Пароль БД'
        required: true
        default: 'root'
        type: string

      student_name:
        description: 'ФИО студента для отчета'
        required: false
        default: 'БГУИР'
        type: string

      student_group:
        description: 'Группа студента для отчета'
        required: false
        default: '[укажите вашу группу]'
        type: string

jobs:
  test:
    name: Интерактивное тестирование проекта
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Клонирование репозитория
      uses: actions/checkout@v4

    - name: ☕ Установка Java JDK 11
      uses: actions/setup-java@v4
      with:
        java-version: '11'
        distribution: 'temurin'
        cache: maven

    - name: 🗄️ Установка MySQL
      run: |
        sudo systemctl start mysql.service
        mysql -e "SELECT VERSION();" -uroot -proot

    - name: 📊 Создание базы данных
      run: |
        # Создаем временную версию init.sql с кастомным именем БД
        sed "s/travel_db/${{ github.event.inputs.db_name }}/g" src/main/resources/init.sql > temp_init.sql
        mysql -u${{ github.event.inputs.db_user }} -p${{ github.event.inputs.db_password }} < temp_init.sql
        echo "✅ База данных '${{ github.event.inputs.db_name }}' создана успешно!"
        mysql -u${{ github.event.inputs.db_user }} -p${{ github.event.inputs.db_password }} -e "USE ${{ github.event.inputs.db_name }}; SELECT COUNT(*) as 'Количество туристов' FROM tourists;"

    - name: ⚙️ Настройка подключения к БД
      run: |
        # Обновляем параметры подключения к БД
        sed -i "s/db.name=.*/db.name=${{ github.event.inputs.db_name }}/g" src/main/resources/database.properties
        sed -i "s/db.user=.*/db.user=${{ github.event.inputs.db_user }}/g" src/main/resources/database.properties
        sed -i "s/db.password=.*/db.password=${{ github.event.inputs.db_password }}/g" src/main/resources/database.properties
        sed -i "s|jdbc:mysql://localhost:3306/[^?]*|jdbc:mysql://localhost:3306/${{ github.event.inputs.db_name }}|g" src/main/resources/database.properties
        echo "✅ Конфигурация БД обновлена:"
        cat src/main/resources/database.properties

    - name: 🔧 Настройка параметров тестирования
      run: |
        # Создаем кастомизированную версию Main.java с пользовательскими параметрами
        sed -i 's/Integer searchId = [0-9]*;/Integer searchId = ${{ github.event.inputs.search_id }};/g' src/main/java/by/bsuir/travel/Main.java
        sed -i 's/newTourist.setFirstName(".*");/newTourist.setFirstName("${{ github.event.inputs.tourist_first_name }}");/g' src/main/java/by/bsuir/travel/Main.java
        sed -i 's/newTourist.setLastName(".*");/newTourist.setLastName("${{ github.event.inputs.tourist_last_name }}");/g' src/main/java/by/bsuir/travel/Main.java
        sed -i 's/newTourist.setEmail(".*");/newTourist.setEmail("${{ github.event.inputs.tourist_email }}");/g' src/main/java/by/bsuir/travel/Main.java
        sed -i 's|newTourist.setPhoneNumber(".*");|newTourist.setPhoneNumber("${{ github.event.inputs.tourist_phone }}");|g' src/main/java/by/bsuir/travel/Main.java
        sed -i 's/newTourist.setDestination(".*");/newTourist.setDestination("${{ github.event.inputs.tourist_destination }}");/g' src/main/java/by/bsuir/travel/Main.java
        sed -i 's/newTourist.setDurationDays([0-9]*);/newTourist.setDurationDays(${{ github.event.inputs.tourist_duration }});/g' src/main/java/by/bsuir/travel/Main.java
        sed -i 's/newTourist.setPrice([0-9.]*);/newTourist.setPrice(${{ github.event.inputs.tourist_price }});/g' src/main/java/by/bsuir/travel/Main.java

        # Обновляем параметры для update
        sed -i 's/newTourist.setDestination("Милан");/newTourist.setDestination("${{ github.event.inputs.update_destination }}");/g' src/main/java/by/bsuir/travel/Main.java
        sed -i 's/newTourist.setDurationDays(10);/newTourist.setDurationDays(${{ github.event.inputs.update_duration }});/g' src/main/java/by/bsuir/travel/Main.java
        sed -i 's/newTourist.setPrice(2100.00);/newTourist.setPrice(${{ github.event.inputs.update_price }});/g' src/main/java/by/bsuir/travel/Main.java

        # Обновляем номер варианта
        sed -i 's/КОНТРОЛЬНАЯ РАБОТА - ВАРИАНТ [0-9]*/КОНТРОЛЬНАЯ РАБОТА - ВАРИАНТ ${{ github.event.inputs.variant_number }}/g' src/main/java/by/bsuir/travel/Main.java

        echo "✅ Параметры тестирования настроены"

    - name: 🔨 Компиляция проекта с Maven
      run: |
        mvn clean compile
        echo "✅ Проект скомпилирован успешно!"

    - name: 🚀 Запуск главного класса и фильтрация вывода
      run: |
        # Запускаем программу и фильтруем только вывод приложения (убираем Maven логи)
        mvn exec:java -Dexec.mainClass="by.bsuir.travel.Main" 2>&1 | \
        grep -v "^\[INFO\]" | \
        grep -v "^\[WARNING\]" | \
        grep -v "^Download" | \
        grep -v "^Progress" | \
        sed '/^$/N;/^\n$/D' > clean_output.txt

        echo "✅ Программа выполнена, вывод сохранен"

    - name: 📋 Создание отчета о выполнении
      run: |
        cat > ОТЧЕТ_КОНТРОЛЬНАЯ_РАБОТА.txt << 'EOF'
        ================================================================================
                          ОТЧЕТ О ВЫПОЛНЕНИИ КОНТРОЛЬНОЙ РАБОТЫ
                                      ВАРИАНТ ${{ github.event.inputs.variant_number }}
        ================================================================================

        Дисциплина: Технологии программирования инфокоммуникационных систем (Часть 2)
        Тема: Туристическое агентство - Управление базой данных клиентов
        Дата выполнения: EOF
        date '+%d.%m.%Y %H:%M:%S' >> ОТЧЕТ_КОНТРОЛЬНАЯ_РАБОТА.txt
        cat >> ОТЧЕТ_КОНТРОЛЬНАЯ_РАБОТА.txt << 'EOF'

        ================================================================================
                                  ЗАДАНИЕ ВАРИАНТА ${{ github.event.inputs.variant_number }}
        ================================================================================

        Реализовать паттерн DAO (Data Access Object) для управления данными
        туристического агентства с использованием JDBC и MySQL.

        ТРЕБУЕМЫЕ МЕТОДЫ:

        1. Travel findEntityById(int id)
           ➜ Назначение: Поиск туриста в БД по уникальному идентификатору
           ➜ Параметр: id - идентификатор туриста
           ➜ Возвращает: объект Travel или null

        2. List<Travel> findAll()
           ➜ Назначение: Получение списка всех туристов из базы данных
           ➜ Возвращает: список объектов Travel

        3. boolean delete(int id)
           ➜ Назначение: Удаление туриста из БД по идентификатору
           ➜ Параметр: id - идентификатор туриста
           ➜ Возвращает: true если удаление успешно, иначе false

        4. boolean delete(Travel tourist)
           ➜ Назначение: Удаление туриста из БД по объекту
           ➜ Параметр: tourist - объект туриста для удаления
           ➜ Возвращает: true если удаление успешно, иначе false

        5. boolean create(Travel tourist)
           ➜ Назначение: Добавление нового туриста в базу данных
           ➜ Параметр: tourist - объект нового туриста
           ➜ Возвращает: true если создание успешно, иначе false

        6. Travel update(Travel tourist)
           ➜ Назначение: Обновление данных существующего туриста
           ➜ Параметр: tourist - объект туриста с обновленными данными
           ➜ Возвращает: обновленный объект Travel или null

        ================================================================================
                              ПАРАМЕТРЫ ИНТЕРАКТИВНОГО ЗАПУСКА
        ================================================================================

        ID для поиска туриста: ${{ github.event.inputs.search_id }}

        Данные нового туриста:
          - Имя: ${{ github.event.inputs.tourist_first_name }}
          - Фамилия: ${{ github.event.inputs.tourist_last_name }}
          - Email: ${{ github.event.inputs.tourist_email }}
          - Телефон: ${{ github.event.inputs.tourist_phone }}
          - Направление: ${{ github.event.inputs.tourist_destination }}
          - Длительность: ${{ github.event.inputs.tourist_duration }} дней
          - Цена: ${{ github.event.inputs.tourist_price }} USD

        Обновленные данные:
          - Направление: ${{ github.event.inputs.update_destination }}
          - Длительность: ${{ github.event.inputs.update_duration }} дней
          - Цена: ${{ github.event.inputs.update_price }} USD

        База данных:
          - Имя БД: ${{ github.event.inputs.db_name }}
          - Пользователь: ${{ github.event.inputs.db_user }}

        ================================================================================
                              ДЕМОНСТРАЦИЯ РАБОТЫ ПРОГРАММЫ
        ================================================================================

        EOF
        cat clean_output.txt >> ОТЧЕТ_КОНТРОЛЬНАЯ_РАБОТА.txt
        cat >> ОТЧЕТ_КОНТРОЛЬНАЯ_РАБОТА.txt << 'EOF'

        ================================================================================
                                    РЕЗУЛЬТАТЫ ВЫПОЛНЕНИЯ
        ================================================================================

        ✅ Все 6 методов TravelDAO успешно реализованы и протестированы
        ✅ Методы работают корректно с базой данных MySQL
        ✅ Используется PreparedStatement для защиты от SQL-инъекций
        ✅ Реализован паттерн DAO для абстракции работы с БД
        ✅ Все соединения с БД корректно закрываются (try-with-resources)

        ================================================================================
                                         ЗАКЛЮЧЕНИЕ
        ================================================================================

        Контрольная работа по варианту ${{ github.event.inputs.variant_number }} выполнена в полном объеме.
        Все требуемые методы реализованы и успешно протестированы.
        Программа готова к демонстрации преподавателю.

        Студент: ${{ github.event.inputs.student_name }}
        Группа: ${{ github.event.inputs.student_group }}

        ================================================================================
        EOF

        echo ""
        echo "✅ Отчет создан: ОТЧЕТ_КОНТРОЛЬНАЯ_РАБОТА.txt"
        echo ""

    - name: 📤 Загрузка отчета как артефакт
      uses: actions/upload-artifact@v4
      with:
        name: Отчет-Контрольная-Работа-Вариант-${{ github.event.inputs.variant_number }}-${{ github.run_number }}
        path: |
          ОТЧЕТ_КОНТРОЛЬНАЯ_РАБОТА.txt
          clean_output.txt
        retention-days: 90

    - name: 📊 Вывод статистики и итогов
      run: |
        echo ""
        echo "╔════════════════════════════════════════════════════════╗"
        echo "║      КОНТРОЛЬНАЯ РАБОТА - ВАРИАНТ ${{ github.event.inputs.variant_number }}             ║"
        echo "║     Туристическое агентство - УСПЕШНО ВЫПОЛНЕНО!      ║"
        echo "╚════════════════════════════════════════════════════════╝"
        echo ""
        echo "📊 Статистика базы данных:"
        mysql -u${{ github.event.inputs.db_user }} -p${{ github.event.inputs.db_password }} -e "USE ${{ github.event.inputs.db_name }};
        SELECT 'Всего туристов:' as 'Параметр', COUNT(*) as 'Значение' FROM tourists
        UNION ALL
        SELECT 'Средняя цена тура:', ROUND(AVG(price), 2) FROM tourists
        UNION ALL
        SELECT 'Средняя длительность:', ROUND(AVG(duration_days), 1) FROM tourists;"
        echo ""
        echo "✅ Все тесты пройдены успешно!"
        echo "📄 Отчет создан: ОТЧЕТ_КОНТРОЛЬНАЯ_РАБОТА.txt"
        echo "📦 Отчет сохранен в артефактах для скачивания"
        echo ""
        echo "💡 Для просмотра полного отчета:"
        echo "   1. Перейдите во вкладку 'Actions' в GitHub"
        echo "   2. Найдите этот запуск workflow"
        echo "   3. Скачайте артефакт 'Отчет-Контрольная-Работа-Вариант-${{ github.event.inputs.variant_number }}-...' "
        echo ""
        echo "📋 Использованные параметры:"
        echo "   - Вариант: ${{ github.event.inputs.variant_number }}"
        echo "   - ID для поиска: ${{ github.event.inputs.search_id }}"
        echo "   - Новый турист: ${{ github.event.inputs.tourist_first_name }} ${{ github.event.inputs.tourist_last_name }}"
        echo "   - Направление: ${{ github.event.inputs.tourist_destination }} → ${{ github.event.inputs.update_destination }}"
        echo "   - База данных: ${{ github.event.inputs.db_name }}"
        echo ""
