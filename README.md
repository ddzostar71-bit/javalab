# Контрольная работа - Вариант 5
## Туристическое агентство - Управление базой данных клиентов

[![Тестирование контрольной работы](https://github.com/ddzostar71-bit/javalab/actions/workflows/test-project.yml/badge.svg)](https://github.com/ddzostar71-bit/javalab/actions/workflows/test-project.yml)

> **🎓 ДЛЯ ДЕМОНСТРАЦИИ ПРЕПОДАВАТЕЛЮ:**
> - **[Как показать работу проекта](DEMO_INSTRUCTION.md)** - инструкция по демонстрации через GitHub Actions
> - **[Документация по CI/CD пайплайну](PIPELINE.md)** - подробное описание работы автоматизации

### Описание проекта

Проект представляет собой полноценное приложение для управления базой данных туристического агентства с применением современных практик разработки на Java. Реализован классический паттерн **DAO (Data Access Object)** для абстракции работы с базой данных, что обеспечивает чистую архитектуру и разделение ответственности между слоями приложения.

### Технологический стек
- **Java 11** - язык программирования
- **JDBC (Java Database Connectivity)** - API для работы с базой данных
- **MySQL 8.0** - реляционная СУБД
- **Maven** - система сборки и управления зависимостями
- **GitHub Actions** - CI/CD для автоматизации тестирования

### Структура проекта
```
javalab/
├── src/
│   └── main/
│       ├── java/
│       │   └── by/bsuir/travel/
│       │       ├── entity/
│       │       │   └── Travel.java          # Entity класс туриста
│       │       ├── dao/
│       │       │   ├── AbstractDAO.java     # Абстрактный класс DAO
│       │       │   └── TravelDAO.java       # Реализация DAO для Travel
│       │       ├── util/
│       │       │   └── ConnectionDB.java    # Управление подключением к БД
│       │       └── Main.java                # Главный класс с демонстрацией
│       └── resources/
│           ├── database.properties          # Настройки подключения к БД
│           └── init.sql                     # SQL скрипт инициализации БД
├── pom.xml                                  # Maven конфигурация
└── README.md
```

### Реализованные методы TravelDAO

В соответствии с заданием контрольной работы (Вариант 5), реализованы следующие методы:

1. **`Travel findEntityById(int id)`** - поиск туриста по ID
2. **`List<Travel> findAll()`** - получение всех туристов из БД
3. **`boolean delete(int id)`** - удаление туриста по ID
4. **`boolean delete(Travel tourist)`** - удаление туриста по объекту
5. **`boolean create(Travel tourist)`** - добавление нового туриста
6. **`Travel update(Travel tourist)`** - обновление данных туриста

---

## Архитектура и техническая реализация

### Паттерн DAO (Data Access Object)

Проект построен на основе классического паттерна DAO, который обеспечивает:
- **Инкапсуляцию** логики доступа к данным
- **Абстракцию** от конкретной реализации БД
- **Повторное использование** кода для работы с базой данных
- **Упрощение тестирования** благодаря четкому разделению слоев

### Структура классов

#### 1. Entity слой (`by.bsuir.travel.entity`)

**Travel.java** - POJO класс, представляющий сущность туриста
```java
public class Travel {
    private Integer id;
    private String firstName;
    private String lastName;
    private String email;
    private String phoneNumber;
    private String destination;
    private Integer durationDays;
    private Double price;
}
```

**Особенности реализации:**
- Все поля инкапсулированы (private) с геттерами/сеттерами
- Реализованы методы `equals()`, `hashCode()` и `toString()` для корректной работы с коллекциями
- Используется обертка `Integer` вместо `int` для поддержки null-значений

#### 2. DAO слой (`by.bsuir.travel.dao`)

**AbstractDAO.java** - абстрактный базовый класс для всех DAO
```java
public abstract class AbstractDAO<T> {
    public abstract List<T> findAll();
    public abstract T findEntityById(Integer id);
    public abstract boolean delete(Integer id);
    public abstract boolean delete(T entity);
    public abstract boolean create(T entity);
    public abstract T update(T entity);
}
```

**TravelDAO.java** - конкретная реализация DAO для работы с туристами

**Ключевые технические решения:**

##### Подготовленные запросы (PreparedStatement)
Все SQL-запросы используют PreparedStatement для защиты от SQL-инъекций:
```java
private static final String SELECT_BY_ID_QUERY = "SELECT * FROM tourists WHERE id = ?";
PreparedStatement statement = connection.prepareStatement(SELECT_BY_ID_QUERY);
statement.setInt(1, id);
```

##### Try-with-resources
Автоматическое управление ресурсами JDBC для предотвращения утечек памяти:
```java
try (Connection connection = ConnectionDB.getConnection();
     PreparedStatement statement = connection.prepareStatement(query)) {
    // работа с БД
} // автоматическое закрытие ресурсов
```

##### Маппинг ResultSet → Entity
Отдельный метод для преобразования результата запроса в объект:
```java
private Travel extractTravelFromResultSet(ResultSet resultSet) throws SQLException {
    Travel tourist = new Travel();
    tourist.setId(resultSet.getInt("id"));
    tourist.setFirstName(resultSet.getString("first_name"));
    // ... остальные поля
    return tourist;
}
```

##### Получение сгенерированных ключей
При создании новой записи автоматически получается сгенерированный ID:
```java
PreparedStatement statement = connection.prepareStatement(INSERT_QUERY,
                                     Statement.RETURN_GENERATED_KEYS);
// ... выполнение запроса
ResultSet generatedKeys = statement.getGeneratedKeys();
if (generatedKeys.next()) {
    tourist.setId(generatedKeys.getInt(1));
}
```

#### 3. Утилиты (`by.bsuir.travel.util`)

**ConnectionDB.java** - управление подключениями к базе данных

**Технические особенности:**
- Использование **ResourceBundle** для внешней конфигурации
- Паттерн **Utility Class** (приватный конструктор, статические методы)
- Централизованное управление параметрами подключения

```java
public class ConnectionDB {
    private static final String DB_PROPERTIES = "database";
    private static ResourceBundle resource = ResourceBundle.getBundle(DB_PROPERTIES);

    public static Connection getConnection() throws SQLException {
        return DriverManager.getConnection(DB_URL, DB_USER, DB_PASSWORD);
    }
}
```

### Обработка ошибок

Реализована надежная обработка исключений на всех уровнях:

```java
try {
    // операция с БД
} catch (SQLException e) {
    System.err.println("Ошибка при операции: " + e.getMessage());
    e.printStackTrace();
    return false; // или null в зависимости от метода
}
```

### Безопасность

1. **Защита от SQL-инъекций** - использование PreparedStatement
2. **Валидация входных данных** - проверка на null перед операциями
3. **Управление ресурсами** - автоматическое закрытие соединений
4. **Экстернализация конфигурации** - пароли в отдельном файле properties

### Преимущества реализации

- **Масштабируемость**: легко добавить новые сущности, наследуя AbstractDAO
- **Поддерживаемость**: четкое разделение ответственности между классами
- **Тестируемость**: бизнес-логика отделена от логики доступа к данным
- **Переносимость**: при смене БД достаточно изменить только DAO-слой

---

## Быстрый старт

### Требования
- Java JDK 11 или выше
- Apache Maven 3.6+
- MySQL Server 8.0

### Установка и запуск

1. **Создание базы данных:**
```bash
mysql -u root -p < src/main/resources/init.sql
```

2. **Настройка подключения:**
Отредактируйте `src/main/resources/database.properties`:
```properties
db.password=ВАШ_ПАРОЛЬ_MYSQL
```

3. **Сборка и запуск:**
```bash
mvn clean compile
mvn exec:java -Dexec.mainClass="by.bsuir.travel.Main"
```

### Альтернативный запуск через IDE
1. Откройте проект в IntelliJ IDEA или Eclipse
2. Импортируйте как Maven проект
3. Запустите класс `Main.java`

### Описание таблицы БД

**Таблица: tourists**
| Поле | Тип | Описание |
|------|-----|----------|
| id | INT | Первичный ключ (AUTO_INCREMENT) |
| first_name | VARCHAR(50) | Имя туриста |
| last_name | VARCHAR(50) | Фамилия туриста |
| email | VARCHAR(100) | Email (уникальный) |
| phone_number | VARCHAR(20) | Номер телефона |
| destination | VARCHAR(100) | Направление/страна |
| duration_days | INT | Длительность тура (дни) |
| price | DECIMAL(10,2) | Стоимость тура |
| created_at | TIMESTAMP | Дата создания записи |

### Примеры использования

```java
TravelDAO travelDAO = new TravelDAO();

// Создание нового туриста
Travel tourist = new Travel();
tourist.setFirstName("Иван");
tourist.setLastName("Петров");
tourist.setEmail("ivan@mail.ru");
tourist.setPhoneNumber("+375291234567");
tourist.setDestination("Париж");
tourist.setDurationDays(7);
tourist.setPrice(1500.00);
travelDAO.create(tourist);

// Получение всех туристов
List<Travel> allTourists = travelDAO.findAll();

// Поиск по ID
Travel found = travelDAO.findEntityById(1);

// Обновление
tourist.setPrice(1600.00);
travelDAO.update(tourist);

// Удаление
travelDAO.delete(tourist.getId());
```

---

## CI/CD и автоматизация

Проект использует **GitHub Actions** для автоматизации тестирования и демонстрации:

- Автоматический запуск при каждом коммите
- Создание и инициализация БД
- Компиляция и тестирование кода
- Генерация отчетов о выполнении

Подробнее см. [PIPELINE.md](PIPELINE.md)

---

## Лицензия и автор

Контрольная работа выполнена в рамках курса **"Технологии программирования инфокоммуникационных систем (Часть 2)"** БГУИР

**Вариант 5**: Туристическое агентство - Управление базой данных клиентов